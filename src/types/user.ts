type User = {
  createdAt: string;
  email: string;
  emailVerifiedAt?: string;
  name: string;
  avatar?: string;
  status: string;
  updatedAt: string;
  username?: string;
  bio?: string;
  _id: string;
};

export type { User };
